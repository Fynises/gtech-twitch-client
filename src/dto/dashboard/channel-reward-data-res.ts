/**
 * channel reward data to be received from API response
 */
export interface ChannelRewardDataResponse {
  id: string;
  title: string;
  image: string;
  backgroundColor: string;
  isEnabled: boolean;
}
